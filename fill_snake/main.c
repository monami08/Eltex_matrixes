#include <stdio.h>
#include "func.h"

int main()
{
    int matrix[N][N];
    /* инициализация матрицы числами "поспирали" */
    initArr (matrix[0]);
    /* Вывод матрицы */
    printArr (matrix[0]);
};